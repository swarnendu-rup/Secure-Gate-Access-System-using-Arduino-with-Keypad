# Secure-Gate-Access-System-using-Arduino-with-Keypad
This embedded system allows secure access control using a 4x4 keypad and password authentication. When the correct password is entered, the servo-controlled gate opens temporarily, accompanied by LED status indicators and user prompts on a 16x2 I2C LCD. Additional features include password masking, visibility toggling, backspace, clear.
